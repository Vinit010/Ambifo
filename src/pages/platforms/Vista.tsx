import PlatformLayout from '../../components/common/PlatformLayout'
import { vistaData } from '../../data/platformsData'

export default function Vista() {
  return <PlatformLayout data={vistaData} />
}
