import PlatformLayout from '../../components/common/PlatformLayout'
import { mipData } from '../../data/platformsData'

export default function MediaIntelligencePlatform() {
  return <PlatformLayout data={mipData} />
}
