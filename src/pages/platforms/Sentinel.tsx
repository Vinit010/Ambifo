import PlatformLayout from '../../components/common/PlatformLayout'
import { sentinelData } from '../../data/platformsData'

export default function Sentinel() {
  return <PlatformLayout data={sentinelData} />
}
